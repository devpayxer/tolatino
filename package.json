{
  "name": "tolatino",
  "private": true,
  "version": "0.0.0",
  "description": "To'Latino — all-in-one local super-app for Latinos in the USA",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "web": "pnpm --filter @tolatino/web"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
