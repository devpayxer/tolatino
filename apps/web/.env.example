# Supabase — public values (safe to expose in the client; Row Level Security
# protects the data). These are baked into the static build at build time.
# Copy to .env.local for local dev; set them as GitHub repo secrets for deploys.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
